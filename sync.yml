name: sync
channels:
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - cudatoolkit=11.8
  - numpy=1.26.3
  - pandas=2.1.4
  - dgl=1.1.1=cuda112py310hc641c19_0
  - pip
  - pip:
    - -r sync_reqs.txt

