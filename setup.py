from setuptools import setup, find_packages

setup(
    name='SynthCoTrain',
    version='0.1.0',
    author='Sasan AmariAmir',
    description='Co-Training for Crystal Synthesizability Prediction',
    packages=find_packages(),
)